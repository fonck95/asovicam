# URL base del backend (Express en Railway). SIN "www." y SIN barra final.
# Las variables definidas en Vercel (Settings → Environment Variables) tienen
# prioridad sobre este archivo; como VITE_* se inyecta en build time, todo
# cambio requiere redeploy.
VITE_API_BASE_URL=https://api.asovicam.org
